meta:
    extract_upstream: False

tasks:
  - source: extract.py
    upstream: []    
    product:
      nb: "products/extract.ipynb"
      json: "products/remote.json"
      nexus: "products/nexus"
    params:
      url: "{{url}}"
      hierarchy: "{{nexus_hierarchy}}"
      single_nexus: "{{single_nexus}}"
      max_substances: "{{max_substances}}"
